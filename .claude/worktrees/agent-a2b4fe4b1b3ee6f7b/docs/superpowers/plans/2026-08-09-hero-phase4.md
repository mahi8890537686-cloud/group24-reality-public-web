# Hero Phase 4 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reduce the hero's height into the brief's range, add a small gold eyebrow label, and simplify the CTA row from 3 buttons to 2, per `docs/superpowers/specs/2026-08-09-hero-phase4-design.md`.

**Architecture:** Single-file change to `src/components/home/HeroSection.tsx`. Task 1 handles layout (height + new eyebrow element). Task 2 handles CTA content (remove two buttons, add one, clean up now-unused imports). Task 3 verifies. `AnimatedHeroBackground.tsx` and `SearchBar.tsx` are not touched by this plan.

**Tech Stack:** Next.js 16.2.12, React 19, `framer-motion`, Tailwind v4 tokens from Phase 1, `lucide-react` icons.

## Global Constraints

- Only `src/components/home/HeroSection.tsx` is touched by this plan. No other file.
- Headline, subheading, location pills, stats row, and scroll indicator content/copy are NOT changed — only the height class, the new eyebrow element, and the CTA row change.
- No test framework exists in this repo — verification is `npx tsc --noEmit` and `npm run lint`.
- Every task ends with a commit.

---

### Task 1: Reduce hero height, add eyebrow label

**Files:**
- Modify: `src/components/home/HeroSection.tsx`

**Interfaces:**
- No prop/behavior changes to the component — it takes no props today and continues to take none.

- [ ] **Step 1: Reduce the section's height**

Replace:
```tsx
    <section
      className="relative min-h-screen flex items-center overflow-hidden"
      aria-label="Hero — Group24 Reality"
    >
```
with:
```tsx
    <section
      className="relative min-h-[85vh] lg:min-h-[90vh] flex items-center overflow-hidden"
      aria-label="Hero — Group24 Reality"
    >
```

- [ ] **Step 2: Add the eyebrow label before the trust badge**

Find:
```tsx
        <div className="max-w-2xl xl:max-w-3xl">
          {/* Trust Badge */}
          <motion.div
            initial={{ opacity: 0, y: -20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.5 }}
            className="inline-flex items-center gap-2 bg-white/10 backdrop-blur-sm border border-white/20 rounded-full px-4 py-2 mb-6"
          >
```
Replace with:
```tsx
        <div className="max-w-2xl xl:max-w-3xl">
          {/* Eyebrow */}
          <motion.p
            initial={{ opacity: 0, y: -20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.5 }}
            className="text-gold text-xs font-inter font-semibold tracking-widest uppercase mb-4"
          >
            Group24 Reality
          </motion.p>

          {/* Trust Badge */}
          <motion.div
            initial={{ opacity: 0, y: -20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.5, delay: 0.05 }}
            className="inline-flex items-center gap-2 bg-white/10 backdrop-blur-sm border border-white/20 rounded-full px-4 py-2 mb-6"
          >
```
(Note the added `delay: 0.05` on the trust badge's `transition` — this makes the eyebrow appear fractionally before it, rather than both animating in simultaneously with identical timing.)

- [ ] **Step 3: Verify**

Run: `npx tsc --noEmit` and `npm run lint`.
Expected: no errors.

Read the file back and confirm: the `<section>` className has `min-h-[85vh] lg:min-h-[90vh]` (not `min-h-screen`), and a new eyebrow `<motion.p>Group24 Reality</motion.p>` renders before the trust badge `motion.div`, with the trust badge's transition now carrying `delay: 0.05`.

- [ ] **Step 4: Commit**

```bash
git add src/components/home/HeroSection.tsx
git commit -m "Reduce hero height to 85-90vh and add GROUP24 REALITY eyebrow label"
```

---

### Task 2: Simplify CTA row to 2 buttons

**Files:**
- Modify: `src/components/home/HeroSection.tsx`

**Interfaces:**
- Consumes: Task 1's edits to the same file (this task's line numbers assume Task 1 has already landed — locate blocks by their literal content shown below, not by absolute line number, since Task 1 shifted everything below its insertion point down by a few lines).

- [ ] **Step 1: Remove the Call Now and WhatsApp buttons, add Schedule a Site Visit**

Find the CTA `motion.div` (three `<a>`/`<Link>` children: "Explore Properties", "Call Now", "WhatsApp"):
```tsx
            <Link
              href="/properties"
              className="group inline-flex items-center gap-2 bg-gold hover:bg-gold-dark text-ink font-inter font-bold px-7 py-4 rounded-xl transition-all duration-200 hover:shadow-xl hover:shadow-gold/30 active:scale-[0.97] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold focus-visible:ring-offset-2 focus-visible:ring-offset-ink text-sm sm:text-base"
            >
              Explore Properties
              <ArrowRight className="w-4 h-4 group-hover:translate-x-1 transition-transform duration-200" />
            </Link>
            <a
              href={PHONE_HREF}
              className="inline-flex items-center gap-2 bg-white/10 hover:bg-white/20 backdrop-blur-sm border border-white/30 text-white font-inter font-semibold px-7 py-4 rounded-xl transition-all duration-200 active:scale-[0.97] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white text-sm sm:text-base"
            >
              <Phone className="w-4 h-4" />
              Call Now
            </a>
            <a
              href={whatsappLink(
                'Hello, I want to enquire about properties in Behror/Neemrana/Kotputli.'
              )}
              target="_blank"
              rel="noopener noreferrer"
              className="inline-flex items-center gap-2 bg-[#25D366]/20 hover:bg-[#25D366]/30 border border-[#25D366]/40 text-white font-inter font-semibold px-7 py-4 rounded-xl transition-all duration-200 active:scale-[0.97] text-sm sm:text-base"
            >
              {/* WhatsApp icon inline */}
              <svg viewBox="0 0 24 24" className="w-4 h-4 fill-[#25D366]" aria-hidden="true">
                <path d="M12 2C6.477 2 2 6.477 2 12c0 1.89.525 3.66 1.438 5.168L2 22l4.98-1.304A9.956 9.956 0 0012 22c5.523 0 10-4.477 10-10S17.523 2 12 2zm0 18c-1.63 0-3.155-.46-4.45-1.26l-.318-.19-3.295.864.88-3.22-.207-.332A7.96 7.96 0 014 12c0-4.418 3.582-8 8-8s8 3.582 8 8-3.582 8-8 8zm4.29-5.93c-.233-.116-1.38-.68-1.593-.758-.214-.078-.37-.116-.525.116-.155.232-.601.758-.737.913-.135.155-.27.174-.502.058-.233-.116-.983-.362-1.873-1.154-.692-.617-1.16-1.38-1.296-1.612-.136-.233-.014-.358.102-.474.105-.104.233-.27.349-.407.116-.135.155-.232.233-.387.078-.155.039-.29-.02-.406-.058-.116-.525-1.265-.72-1.732-.19-.455-.383-.393-.525-.4H8.7c-.155 0-.406.058-.618.29-.213.232-.812.794-.812 1.934 0 1.14.831 2.242 1.947 3.358 1.116 1.116 2.242 1.947 3.358 1.947.564 0 1.14-.193 1.586-.58.446-.386.735-.91.75-1.274.015-.36-.019-.66-.136-.776z" />
              </svg>
              WhatsApp
            </a>
```
Replace with:
```tsx
            <Link
              href="/properties"
              className="group inline-flex items-center gap-2 bg-gold hover:bg-gold-dark text-ink font-inter font-bold px-7 py-4 rounded-xl transition-all duration-200 hover:shadow-xl hover:shadow-gold/30 active:scale-[0.97] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gold focus-visible:ring-offset-2 focus-visible:ring-offset-ink text-sm sm:text-base"
            >
              Explore Properties
              <ArrowRight className="w-4 h-4 group-hover:translate-x-1 transition-transform duration-200" />
            </Link>
            <Link
              href="/contact"
              className="inline-flex items-center gap-2 bg-white/10 hover:bg-white/20 backdrop-blur-sm border border-white/30 text-white font-inter font-semibold px-7 py-4 rounded-xl transition-all duration-200 active:scale-[0.97] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-white text-sm sm:text-base"
            >
              Schedule a Site Visit
            </Link>
```

- [ ] **Step 2: Remove now-unused imports**

Find:
```tsx
import { Phone, ArrowRight, MapPin, Shield } from 'lucide-react';
import { PHONE_HREF, whatsappLink } from '@/lib/utils';
```
Replace with:
```tsx
import { ArrowRight, MapPin, Shield } from 'lucide-react';
```
(`Phone` was only used by the removed "Call Now" button; `PHONE_HREF` and `whatsappLink` were only used by the removed "Call Now"/"WhatsApp" buttons. Confirm none of the three are referenced anywhere else in this file before removing — if any is still used elsewhere, keep that specific one and only drop the ones that are genuinely unused.)

- [ ] **Step 3: Verify**

Run: `npx tsc --noEmit` and `npm run lint`.
Expected: no errors, and specifically no unused-import warnings for `Phone`, `PHONE_HREF`, or `whatsappLink`.

Read the file back and confirm: exactly two CTA elements render (Explore Properties, Schedule a Site Visit), no leftover `PHONE_HREF`/`whatsappLink`/`Phone` references anywhere in the file, and the inline WhatsApp SVG `<path>` is fully removed.

- [ ] **Step 4: Commit**

```bash
git add src/components/home/HeroSection.tsx
git commit -m "Simplify hero CTA row to Explore Properties + Schedule a Site Visit"
```

---

### Task 3: Build verification and manual smoke check

**Files:**
- No modifications expected — this task verifies Tasks 1-2.

**Interfaces:**
- Consumes: the complete Task 1 + Task 2 `HeroSection.tsx`.

- [ ] **Step 1: Full build check**

Run: `npx tsc --noEmit`, `npm run lint`, `npm run build`.
Expected: all three succeed with no errors.

- [ ] **Step 2: Manual smoke check**

Run `npm run dev`, then in a browser (or via the `claude-in-chrome` tools if available) at both a mobile viewport (375-430px) and a desktop viewport (≥1280px):
- Load `/`: confirm the hero is visibly shorter than full-height — some of the search card below should be at least partially visible without scrolling, on both mobile and desktop.
- Confirm the gold "GROUP24 REALITY" eyebrow appears above the trust-badge pill, both animating in during the entrance sequence.
- Confirm exactly two CTA buttons render: "Explore Properties" (gold-filled) and "Schedule a Site Visit" (outline/translucent) — no "Call Now" or "WhatsApp" button anywhere in the hero.
- Click "Explore Properties": confirm it navigates to `/properties`. Click "Schedule a Site Visit": confirm it navigates to `/contact`.
- Confirm headline, subheading, location pills, stats row, and scroll indicator are all visually unchanged from before this phase.
- Enable "reduce motion" in OS/browser settings, reload: confirm the hero still renders correctly (no broken layout), even though entrance animations are reduced/skipped.

- [ ] **Step 3: Commit (only if Step 1 or Step 2 surfaced a fix)**

If verification is clean, no commit needed for this task.

---

## Definition of Done

- Hero section height is `min-h-[85vh] lg:min-h-[90vh]`, not `min-h-screen`.
- A gold "GROUP24 REALITY" eyebrow renders above the existing trust-badge pill.
- Exactly two CTA buttons render in the hero: "Explore Properties" (`/properties`) and "Schedule a Site Visit" (`/contact`).
- No unused imports remain (`Phone`, `PHONE_HREF`, `whatsappLink` all removed if no longer referenced).
- `npx tsc --noEmit`, `npm run lint`, `npm run build` all pass.
- Headline, subheading, location pills, stats row, scroll indicator, and `AnimatedHeroBackground`/`SearchBar` are all unchanged from before this phase.
